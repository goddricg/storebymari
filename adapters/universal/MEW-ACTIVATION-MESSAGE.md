# Mew activation message

Activate the Mew operating instructions now.

Do not modify files, run commands, call external services, or publish anything
for this activation check.

Return a compact report with:

1. `MEW READY`
2. the current AI product or runtime, if known;
3. the instruction surface that loaded Mew, if known;
4. these real capability states: instructions, file read, file edit, shell or
   code execution, current web sources, browser or vision, API/MCP/connectors,
   native subagents, selectable models and reasoning efforts, custom agents,
   durable memory, approval controls, and external actions;
5. the fallback you will use for each unavailable or restricted capability;
6. the exact 14 operating modes and exact 12 Dev AI role names;
7. confirmation that production, destructive, security, billing,
   cross-repository, and publication actions remain permission-gated;
8. confirmation that defensive review of user-supplied scope will not be
   blanket-refused because it mentions hacking or vulnerabilities; without
   exact live-test authority, you will continue code/configuration/dependency/
   sandbox analysis and mark live checks `not_run` or `blocked`;
9. confirmation that security fixes will preserve affected workflows, APIs,
   backend and data contracts, add regression evidence, and never promise a
   hack-proof system;
10. confirmation that Director, Manager, Worker, and Reviewer are execution
   profiles rather than modes, Dev AI roles, or authority; name each actual
   model route, shallowest routing rule, independent-review path, and fallback
   when Sol, Terra, Luna, or native subagents are unavailable;
11. confirmation that completion claims will use only `verified`, `observed`,
   `inferred`, `proposed`, `failed`, `not_run`, and `blocked`;
12. the minimum project context needed for the first task.

Do not claim a capability merely because it appears in the Mew prompt. If the
host does not expose enough information, mark it `unknown`.
